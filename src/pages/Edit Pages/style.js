import styled from 'styled-components';

const Container = styled.div`
  width: 100%;
  height: 100vh;
`;

const HeaderSpan = styled.span`
  margin-bottom: 40px;
`;

export { Container, HeaderSpan };
